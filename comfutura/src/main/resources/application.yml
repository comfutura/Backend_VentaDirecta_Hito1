spring:
  datasource:
        url: jdbc:mysql://localhost:3306/comfutura?useSSL=false&serverTimezone=America/Lima
        username: root
        password: mysql
        driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate          # cambia a 'update' si quieres que Hibernate cree/modifique tablas automáticamente
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: ${PORT:8080}

security:
  jwt:
    secret-key: 586B633834416E396D7436753879382F423F4428482B4C6250655367566B5970
    expiration-minutes: 480